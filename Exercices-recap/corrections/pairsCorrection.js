let i = 0 // Initialisation de la variable
while (i <= 20) {
	// Condition de la boucle : tant que i est inférieur ou égal à 20
	if (i % 2 === 0) {
		// Vérification si i est pair
		console.log(i) // Affichage du nombre pair
	}
	i++ // Incrémentation de i de 1 à chaque itération
}
